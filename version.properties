# App version — bump before every Play Store submission.
#
# versionCode : integer, must be strictly greater than the previously
#               published version. Increment by 1 for each release.
#
# versionName : user-visible string shown on the Play Store listing.
#               Follow Semantic Versioning: MAJOR.MINOR.PATCH
#                 MAJOR — breaking change / full redesign
#                 MINOR — new features (backwards-compatible)
#                 PATCH — bug fixes only
#
VERSION_CODE=1
VERSION_NAME=1.2.0
